version: '3.8'

services:
  web:
    build:
      context: .
      args:
        REACT_APP_API_BASE_URL: ${REACT_APP_API_BASE_URL:-https://admin.bttrades.com}
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: always